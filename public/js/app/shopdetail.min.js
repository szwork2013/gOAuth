/**
* 加载入口
***/
function init() {
	// 页面布局定位
	$(".app_bottom").height(document.documentElement.clientHeight - 349);
	$(".app_bottom .bottom_bottom").height($(".app_bottom").height() - 84);
	// 自定义滚动条
	$scroll.init("#bottom_bottom", function() {
	});
}